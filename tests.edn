#kaocha/v1
    {:tests    [{:id          :unit
                 :test-paths  ["test"]}]
     :fail-fast? false
     :color?     true
     :plugins  [:kaocha.plugin/randomize
                :kaocha.plugin/notifier]
     :reporter [kaocha.report.progress/report]}
